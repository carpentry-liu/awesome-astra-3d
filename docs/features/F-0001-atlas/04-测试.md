# 实际验证

日期：2026-09-06；Windows、PowerShell 7、本机 Node 24.14.0；构建兼容运行时 Node 22.22.0。

## 数据与行为

`npm run check`：41 条记录校验通过；7 项 node:test 通过；TypeScript 无错误。覆盖 Astra/参考隔离、多词搜索、交叉筛选、空结果、WebMCP 非法参数拒绝、转载日期与视频入口语义。

首次 `npm run build`：静态文件已产出，但 Node 24.14.0 在退出时触发 `UV_HANDLE_CLOSING` 断言，退出 1，未作为成功发布依据。

`npm exec --yes --package=node@22.22.0 -- node node_modules/vinext/dist/cli.js build`：退出 0，5 个构建阶段与静态预渲染成功，`dist/client/index.html` 存在。后续统一由兼容构建入口执行。

## 真实浏览器

通过 Codex 浏览器在 `http://localhost:5173/` 实测，使用同一页面和开发会话。

- 1440×1000：无水平溢出，主要来源图片实际加载；[宽屏截图](evidence/desktop.png)。
- 390×844：无水平溢出，筛选换行正常；[移动截图](evidence/mobile.png)。
- 输入 `SIMON blender`：显示 1 个结果；详情包含作者、日期、模型、源码和过程；[详情截图](evidence/detail.png)。
- 点击复制案例：剪贴板实际得到 `#case=simonw-pelican-bicycle` 深链接。
- 切换方法参考：12 个结果，Astra 计数仍为 29。
- 输入不存在的词：出现真实空状态；清除筛选恢复结果。
- 移动端详情无水平溢出，关闭按钮可用；[移动详情](evidence/mobile-detail.png)。
- 浏览器 `dev.logs({levels:['error','warn']})` 两次返回 `[]`。
- WebMCP 实际注册 `search_astra_cases`；`{query:'Simon Blender'}` 返回 1 条作者案例，`{group:'invalid'}` 明确拒绝。只读工具没有外部写入。

## 来源限制

官方 12 个图片和 11 个在线作品 URL 由研究核验 HEAD 200；参考仓库 14 个图片 URL HEAD 200；Gogh Strike 截图 HEAD 200。此处是可访问性检查，不能视为作品功能、建模质量或生成速度复现。

X 主要通过原文镜像/转引读取，证据等级明确保留。小红书：公开精确检索未取得案例原帖；浏览器读取两次超时；专用本地 MCP `check_login_status` 返回“未登录”。当前不收录任何小红书笔记。

依赖安装保留官方脚手架锁定版本。安装时 npm 报告 11 条依赖安全提示（1 low、2 moderate、8 high）；没有运行破坏性 `audit fix --force`。部署仅含静态公开产物，不部署开发服务、构建工具或 Worker。

最终统一 `npm run build`：兼容运行时构建退出 0；`npm run lint`：原创 app/src/scripts/tests 退出 0。全树首次 lint 发现脚手架供应商组件的现有规则冲突，未改供应商代码；原创代码的图片直链策略作有理由的单规则豁免，首页锚点和搜索标签已修正。

另补 4 张 X 案例视频封面，均从 eChai 对应 article 的实际 video.poster 提取，HEAD 200 image/jpeg；图注明示转引，保持转引待复核级别。剩余 4 条没有独立可用图片，保留明确回源状态，不替用其他作品。

部署回执与最终远端验证在完成后追加，不把构建成功视为部署成功。
