# 验证记录

环境：Windows，项目独立 Git 根，Node 24.14.0；项目构建脚本使用 Node 22.22.0 完成 Vinext 静态导出。

| 命令／操作 | 实际结果 |
| --- | --- |
| `npm run check` | 162 条记录通过来源、日期、URL 和媒体验证；11 个行为测试通过；TypeScript 通过 |
| `npm run lint` | 通过 |
| `npm run build` | 通过；导出首页及 9 个资源引用检查成功 |
| Sites `build-site.mjs` | Windows 下寻找不存在的本地 npm 入口而失败；采用项目原有且已通过的 `npm run build`，未替换依赖 |
| 18 个下载文件 | 9 段录像的原片及播放版，全部全长解码；媒体 ID、时长差、字节数与 SHA-256 已核查 |
| GitHub Release | `media-2026-09-11-evening` 已公开，18 个文件，379,654,520 字节；已有视频归档保留 |
| 历史保留检查 | 原有 147 条记录全部保留；只改五条官方案例的图片地址、观察日期和读取说明 |
| 链接复查 | 当前快照 400 个地址：397 可达，3 张 Reddit 图片返回 403；不据单次限制删除作者作品 |
| GitHub Markdown 渲染 | 中文 README 正确生成 9 张图片：6 张原作者预览返回图像类型和 206，3 张本地实拍存在；中英文共用相同图片 |

浏览器检查覆盖 1440 × 1100 桌面与 390 × 844 手机：三件精选显示真实作者画面；MuJoCo 搜索命中厨房；GENESIS 与源码组合筛选命中一条，卡片明确标为分享卡片；Enter 打开详情、Escape 关闭。手机文档宽度 375，无横向溢出。控制台错误／警告为零。

截图为浏览器原生 JPEG：桌面和案例区实际 1425 × 1089，手机 375 × 812。首页显示 150 个案例、49 个工程、59 个演示、75 个完整视频。见 [截图索引](../../media/README.md)。

演示入口只完成有界 HTTP、标题和最终地址检查，未逐一试玩；第三方工程、科学内容、几何精度和作者耗时未独立复现。新归档视频的线上字节比对及双站发布状态在本次执行的发布证据中另行核对，不能把本地检查当成线上成功。
