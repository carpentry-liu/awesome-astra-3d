# 更新方案

方案 A：只增加社媒条目和计数。实施快，但工程资料仍分散。

方案 B（采用）：补充来源明确的作品，同步三件首页精选、README 六图与真实桌面／手机截图；优先展示可编辑工程和作者复盘。沿用现有深色图库、筛选和详情，不新增依赖或重做视觉系统。

事实仍只写入 data/cases.json，未知字段保留 null。新记录前置到既有官方记录之后，让同日第二批内容也能在最新收录中出现。保留此前 176 条记录的完整字段。

新增 X 视频下载最高码率及较小的完整平台版本，验证时长、全长解码、字节数与 SHA-256，使用独立 media-2026-09-12-second Release；不裁剪或转码。其他作者图片保持外链，输入参考图不冒充输出。

新截图采用独立 second 文件名，保留前一批截图。更新中英文 README、About 和生成目录。验证 check、lint、build、实际页面宽屏与手机布局、控制台和播放，发布 GitHub Pages 与既有 Sites。只重查新增或触及的链接，保留旧检查日期。回滚以本次独立提交为界，不删除历史媒体归档。

实拍发现探测车对照图被现有 cover 缩放裁掉两侧。采用既有 comparison 媒体类型，列表预览改用 contain 保留完整对照范围；同类型历史图也按这一规则显示。其他图片及详情布局保持原样。此变更通过实际对照图和移动布局验证，不增加镜像 CSS 实现的测试。
