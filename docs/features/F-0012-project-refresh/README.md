# F-0012 · 2026-09-14 日更

用户要求更新今日内容，并明确不部署 ChatGPT 站点。本轮只更新 GitHub 内容与既有 GitHub Pages，保留 ChatGPT / Sites 已发布版本；不向 Sites 源仓库推送，也不保存或部署 Sites 版本。

基线 190 条记录（178 Astra + 12 参考），工作区开始时干净。采用现有数据结构、主题与发布工作流，不新增功能或依赖。

## 调研与方案

从 [Tripo 案例索引](https://www.tripo3d.ai/3d-prompts/models/gpt-6-astra)、X 精确原帖镜像、GitHub 最新更新仓库与 Reddit 作者页面交叉检索。索引只用于发现，使用原作者文字和实际文件确认归因、输出及完成程度。收录日期与发表日期分开，不将更新时间作为作品生成时间。

方案一是仅刷新日期和封面，无法提供新的创作材料；方案二是核清新增作品、原片与可访问工程，同步图库和 README，沿用现有验证。采用方案二，保持改动范围集中在内容。

X 视频保存最高码率与较小完整版本，验证时长、SHA-256 和全片解码；不裁剪或转码。参考图不作为输出，公开文件不自动视为自由授权。

## 实施与验证

新增 5 条：锦鲤池、City Pulse 纽约出租车地图、已有 Tripo 模型 UV 重整、程序化鱿鱼群、Belt Runner Godot 移植。四条 X 保持精确 ID 镜像的转引级别；Godot 归因仅覆盖作者明确说明的 Astra Blender 资产。未公开工程或演示的案例不补造入口。

候选 `surefirev2/ai-blender-workflow` 的 README、模型文件和实际渲染最初可读，但 01:05 UTC 的最终 HTTP 检查中，仓库与原始图片均变为 404；另一次网页读取确认失败。发布前撤下候选与对应封面，不依赖浏览器缓存。AstraPool 等缺少明确创作归因的项目也没有加入；具体线索见 `data/research.json`。

最终 195 条记录 = 183 Astra + 12 方法参考，55 份源码／工程、65 个演示入口、89 段完整视频。190 条旧案例和 172 个旧媒体条目逐项保持不变。新 3 段录像各存最高码率和较小完整版本，共 6 个 MP4、175,434,081 字节；全库网页播放版本合计 337,388,681 字节。

| 验证 | 实际命令与结果 |
| --- | --- |
| 数据与行为 | `npm run check`：195 条数据、11 项行为测试、TypeScript 全部通过 |
| 构建 | `npm run build`：成功预渲染 2 路由，验证首页和 9 项资源引用；保留既有大 chunk、插件耗时与 DEP0190 提示 |
| 目录 | `npm run catalog`：中英文统计、CATALOG、UPDATES、公开 JSON 和 sitemap 从事实源同步 |
| 视频 | `work/download-sep14.py`、`work/download-squid.py`：6 文件全片解码成功，SHA-256、字节数、平台时长差不超过 0.2 秒 |
| 增量链接 | `work/audit-sep14.py`：10 个候选链接中 8 个可达，2 个 404 导致撤下候选；旧检查日期保留 |
| 旧数据 | `work/document-sep14.py`：190 条旧案例与 172 个旧视频记录逐项一致，新增 media ID 无重复 |
| 差异 | `git diff --check`：无空白错误 |

本地真实浏览器检查：桌面 1440 × 1100，图片均已加载；手机 390 × 844，文档宽与滚动宽同为 375，没有横向溢出。最新收录显示五条新增，控制台 error / warn 均为空，临时视口覆盖已恢复。使用既有布局，未修改依赖、主题或架构。

中英文 README 更新六图画廊（四张新作品、两张延续精选），首页换成锦鲤池、UV 流程和 City Pulse；Belt Runner 无实际截图，保留来源占位与工程入口。三张原生 JPEG 实拍：

- [首页](../../media/homepage-2026-09-14.jpg)：158,997 字节，1425 × 1089。
- [最新收录](../../media/collection-2026-09-14.jpg)：180,481 字节，1425 × 1089。
- [手机](../../media/mobile-2026-09-14.jpg)：55,793 字节，375 × 812。

## 发布

3 段完整原片及对应播放版本已发布至 [2026-09-14 视频归档](https://github.com/carpentry-liu/awesome-astra-3d/releases/tag/media-2026-09-14)。文件保留来源、时长和校验值；媒体版权归原作者。

GitHub Pages 发布及线上检查完成后补充结果。ChatGPT 站点本次不部署，也未向 Sites 源仓库推送或创建版本。
