# 验证记录

实际运行环境：Windows、Node 24.14.0；既有构建脚本使用官方 Node 22.22.0 完成静态预渲染。

| 检查 | 实际命令或操作 | 结果 |
| --- | --- | --- |
| 数据及行为 | npm run check | 147 条记录校验；11 个行为测试与 TypeScript 通过 |
| 静态检查 | npm run lint | 通过；修复精选标题中的 JSX 引号转义后复查 |
| 构建 | npm run build | 通过，预渲染 2 个路由；导出首页及 9 个资源引用校验通过 |
| GitHub Markdown | GitHub Markdown API，mode=gfm，当前 README | 六张作品图、两张本地截图均有效；六张外部图返回 image/jpeg |
| 桌面 | Codex 浏览器 1440 × 1100 | 三件精选同时可见；侧栏、三列最新案例、搜索布局已实拍 |
| 手机 | Codex 浏览器 390 × 844 | 单列精选，横向分类及来源筛选；无页面横向溢出，三张精选图加载成功 |
| 组合筛选 | Houdini + 程序化场景 + X + 完整视频 | 1 条结果；URL 携带所有参数，刷新并完成客户端初始化后恢复同一结果 |
| 键盘详情 | Houdini 卡片 Enter；详情 Escape | 标题、证据、视频阶段与下载入口可读；关闭后对话框数量 0 |
| 额外重排 | 720 × 550 | document.scrollWidth 705，innerWidth 720，无页面横向溢出 |
| 浏览器放大 | Ctrl+0 与五次 Ctrl+plus | 当前内嵌浏览器未改变报告的视口与 DPR，不能计为真正 200% 缩放通过；另做 720px 重排检查 |
| 视频文件 | 原片与完整播放版均使用 ffmpeg 全长解码 | 16 个文件通过；时长误差均小于 0.2 秒，无失败 |

Sites 的 build-site.mjs 在本机调用 npm 包装脚本时解析到不存在的项目内 node_modules/npm；使用项目既有 npm run build 成功，未改变依赖或构建路径。无浏览器 JavaScript 错误的检查在打开尚未上线的新视频前完成；正式媒体 URL 和视频播放在发布后单独验证。

本文件记录发布前检查。正式部署返回状态、线上目录一致性、媒体抽查与播放结果保存在本轮本地工作记录，并在交付时报告；不将已提交或上传视为已上线。公开发布过程可在 [Pages 工作流](https://github.com/carpentry-liu/awesome-astra-3d/actions/workflows/pages.yml) 核查。
