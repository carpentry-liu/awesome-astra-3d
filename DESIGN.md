# 产品与技术设计

## 产品定位

Astra 3D Atlas 收集 GPT-6 Astra 建模、交互世界与空间可视化作品，供创作者浏览结果、追溯原帖和参考公开提示词。中文为主，保留作品英文名称。

## 信息结构

案例区以可视化作品为核心，提供关键词和分类检索。每条包含作者、平台、来源、模型证据等级、输出形式、观察日期、原始日期、提示词公开情况、运行或源码链接。官方发布、作者自述、二手转述、邻近模型分开标注；不声称独立复现。

按材料提供源码、作者演示入口与完整视频筛选，可与类别、作者搜索和来源组合。筛选状态写入 URL，保留传播参数；最新排序使用 `addedAt` 收录日期，与作品日期、核查日期分开。README 中英入口、更新目录和页面统计都从事实源同步。

参考仓库的 GPT Image 2 案例独立收录为方法参考，默认 Astra 视图不混入。检索未命中的平台写入覆盖记录，不用无关内容凑数。

## 方案比较

1. 静态 JSON + React/Vinext 静态导出：无需账号与数据库，可 GitHub Pages 和 Sites 双部署，适合公开可审查目录。采用。
2. 数据库 + 后台投稿：支持多人编辑，但引入审核和持久化成本，当前需求没有消费者。暂不采用。

## 视觉

采用空间作品档案馆：墨黑底、亮蓝强调、窄边框、清晰编号与宽幅案例图片。欢迎区以作者作品为主视觉，提供三件精选的档案与演示 / 过程入口；四项资源统计连接案例筛选，接续原有案例索引。正文至少 16px，交互有键盘焦点，尊重减少动态效果设置。

## 边界与回滚

第三方图片仅外链展示并标注来源；失败显示明确的原帖入口。无身份认证、自动抓取、自动发帖和大模型 API 调用。Git 提交可回滚网站与同版本数据，静态部署可回退历史版本。

2026-09-08：首页提取为 `app/welcome.tsx`，README 的统计和最新八条记录统一由 `scripts/generate-catalog.mjs` 生成。演示可达性快照记录于 `data/demo-audit.json`，不将 HTTP 200 当作完成试玩。
